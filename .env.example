# Valyra environment — copy to .env.local and fill in.
# Supabase is wired up in a later step; these are placeholders for now.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only key — never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=
